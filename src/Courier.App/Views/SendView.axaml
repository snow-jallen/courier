<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="using:Courier.App.ViewModels"
             x:Class="Courier.App.Views.SendView"
             x:DataType="vm:SendViewModel">

  <StackPanel Margin="28,26,28,34">
    <TextBlock Classes="h2" Text="Send a message"/>
    <TextBlock Classes="lede" Margin="0,4,0,0"
               Text="Choose who it goes to, write it once, and each person gets it the way they prefer."/>

    <TextBlock Classes="eyebrow" Text="WHO GETS THIS" Margin="0,20,0,9"/>

    <!-- Filters -->
    <Grid ColumnDefinitions="*,Auto,Auto,Auto,Auto,Auto" ColumnSpacing="8" Margin="0,0,0,10">
      <TextBox Grid.Column="0" MinWidth="180" PlaceholderText="Search a name, email or phone"
               Text="{Binding Search}"/>
      <ComboBox Grid.Column="1" MinWidth="150" ItemsSource="{Binding WardOptions}"
                SelectedItem="{Binding Ward}"/>
      <ComboBox Grid.Column="2" MinWidth="140" ItemsSource="{Binding ChannelOptions}"
                SelectedItem="{Binding Channel}"/>
      <ComboBox Grid.Column="3" MinWidth="120" ItemsSource="{Binding MonthOptions}"
                SelectedItem="{Binding BirthdayMonth}"/>
      <StackPanel Grid.Column="4" Orientation="Horizontal" Spacing="5" VerticalAlignment="Center">
        <TextBlock Text="Age" VerticalAlignment="Center" FontSize="12.5"
                   Foreground="{DynamicResource Ink2}"/>
        <TextBox Width="56" Text="{Binding MinAge}" PlaceholderText="any"/>
        <TextBlock Text="to" VerticalAlignment="Center" FontSize="12.5"
                   Foreground="{DynamicResource Ink2}"/>
        <TextBox Width="56" Text="{Binding MaxAge}" PlaceholderText="any"/>
      </StackPanel>
      <Button Grid.Column="5" Content="Clear" Command="{Binding ClearFiltersCommand}"/>
    </Grid>

    <!-- Selection bar + list -->
    <Border Background="{DynamicResource AccentSoft}" CornerRadius="9,9,0,0" Padding="13,9"
            BorderBrush="{DynamicResource Line}" BorderThickness="1,1,1,0">
      <Grid ColumnDefinitions="Auto,Auto,*,Auto">
        <Button Grid.Column="0" Content="Select all" Padding="10,3" FontSize="12.5"
                Command="{Binding SelectAllCommand}"/>
        <Button Grid.Column="1" Content="Clear selection" Padding="10,3" FontSize="12.5"
                Margin="7,0,0,0" Command="{Binding SelectNoneCommand}"/>
        <TextBlock Grid.Column="3" Text="{Binding SelectedLine}" FontWeight="SemiBold"
                   VerticalAlignment="Center" Foreground="{DynamicResource AccentInk}"/>
      </Grid>
    </Border>

    <Border Classes="card" CornerRadius="0,0,9,9" Padding="0" ClipToBounds="True">
      <StackPanel>
        <Border Classes="header">
          <Grid ColumnDefinitions="36,2*,1.4*,0.5*,0.8*,0.8*,2*">
            <TextBlock Grid.Column="0" Classes="colhead" Text=""/>
            <Button Grid.Column="1" Classes="sortable" Content="NAME"
                    Command="{Binding SortByCommand}" CommandParameter="name"/>
            <Button Grid.Column="2" Classes="sortable" Content="WARD"
                    Command="{Binding SortByCommand}" CommandParameter="ward"/>
            <Button Grid.Column="3" Classes="sortable" Content="AGE"
                    Command="{Binding SortByCommand}" CommandParameter="age"/>
            <Button Grid.Column="4" Classes="sortable" Content="BIRTHDAY"
                    Command="{Binding SortByCommand}" CommandParameter="birthday"/>
            <Button Grid.Column="5" Classes="sortable" Content="PREFERS"
                    Command="{Binding SortByCommand}" CommandParameter="channel"/>
            <TextBlock Grid.Column="6" Classes="colhead" Text="GOES TO"/>
          </Grid>
        </Border>
        <ScrollViewer MaxHeight="290">
          <ItemsControl ItemsSource="{Binding Rows}">
            <ItemsControl.ItemTemplate>
              <DataTemplate x:DataType="vm:SendRow">
                <Border Classes="row">
                  <Grid ColumnDefinitions="36,2*,1.4*,0.5*,0.8*,0.8*,2*">
                    <CheckBox Grid.Column="0" IsChecked="{Binding IsSelected}" VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="1" Text="{Binding Name}" FontWeight="SemiBold"
                               Foreground="{DynamicResource Ink}" VerticalAlignment="Center"
                               TextTrimming="CharacterEllipsis"/>
                    <TextBlock Grid.Column="2" Text="{Binding Ward}" FontSize="12.5"
                               Foreground="{DynamicResource Ink3}" VerticalAlignment="Center"
                               TextTrimming="CharacterEllipsis"/>
                    <TextBlock Grid.Column="3" Text="{Binding Age}" Classes="mono"
                               Foreground="{DynamicResource Ink2}" VerticalAlignment="Center"/>
                    <TextBlock Grid.Column="4" Text="{Binding Birthday}" Classes="mono"
                               Foreground="{DynamicResource Ink3}" VerticalAlignment="Center"/>
                    <Panel Grid.Column="5" HorizontalAlignment="Left" VerticalAlignment="Center">
                      <Border CornerRadius="20" Padding="8,2" Background="{DynamicResource ChEmailSoft}"
                              IsVisible="{Binding IsEmail}">
                        <TextBlock Text="Email" FontSize="11.5" FontWeight="SemiBold"
                                   Foreground="{DynamicResource ChEmail}"/>
                      </Border>
                      <Border CornerRadius="20" Padding="8,2" Background="{DynamicResource ChTextSoft}"
                              IsVisible="{Binding IsText}">
                        <TextBlock Text="Text" FontSize="11.5" FontWeight="SemiBold"
                                   Foreground="{DynamicResource ChText}"/>
                      </Border>
                      <Border CornerRadius="20" Padding="8,2" Background="{DynamicResource ChVoiceSoft}"
                              IsVisible="{Binding IsVoice}">
                        <TextBlock Text="Voice" FontSize="11.5" FontWeight="SemiBold"
                                   Foreground="{DynamicResource ChVoice}"/>
                      </Border>
                      <Border CornerRadius="20" Padding="8,2" Background="{DynamicResource Surface3}"
                              IsVisible="{Binding NoChannel}">
                        <TextBlock Text="Not set" FontSize="11.5" FontWeight="SemiBold"
                                   Foreground="{DynamicResource Ink3}"/>
                      </Border>
                    </Panel>
                    <StackPanel Grid.Column="6" Orientation="Horizontal" Spacing="5"
                                VerticalAlignment="Center">
                      <TextBlock Text="&#x26A0;" FontSize="12" IsVisible="{Binding !CanReceive}"
                                 Foreground="{DynamicResource Crit}" VerticalAlignment="Center"/>
                      <TextBlock Text="{Binding GoesTo}" Classes="mono"
                                 TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
                    </StackPanel>
                  </Grid>
                </Border>
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
        </ScrollViewer>
      </StackPanel>
    </Border>
    <TextBlock Text="{Binding MatchLine}" Margin="0,8,0,0" FontSize="12"
               Foreground="{DynamicResource Ink3}"/>

    <!-- Compose + summary -->
    <Grid ColumnDefinitions="*,310" ColumnSpacing="18" Margin="0,22,0,0">
      <StackPanel Grid.Column="0" Spacing="14">
        <StackPanel Spacing="6">
          <TextBlock Classes="eyebrow" Text="SUBJECT — EMAIL ONLY"/>
          <TextBox Text="{Binding Subject}" PlaceholderText="Stake singles dinner — Friday at 6:30"/>
        </StackPanel>
        <StackPanel Spacing="6">
          <TextBlock Classes="eyebrow" Text="MESSAGE"/>
          <TextBox Text="{Binding Body}" AcceptsReturn="True" Height="130"
                   TextWrapping="Wrap"
                   PlaceholderText="What you want everyone to know."/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Spacing="9">
          <Button Classes="primary" Content="{Binding SendLabel}"
                  Command="{Binding SendCommand}" IsEnabled="{Binding !Sending}"/>
        </StackPanel>
        <TextBlock Text="{Binding Status}" TextWrapping="Wrap" FontSize="12.5"
                   Foreground="{DynamicResource Ink2}"/>
      </StackPanel>

      <Border Grid.Column="1" Background="{DynamicResource Surface2}" CornerRadius="9"
              BorderBrush="{DynamicResource Line}" BorderThickness="1" Padding="14"
              VerticalAlignment="Top">
        <StackPanel Spacing="11">
          <TextBlock Classes="eyebrow" Text="HOW IT GOES OUT"/>

          <Grid ColumnDefinitions="60,*,30">
            <TextBlock Grid.Column="0" Text="Email" FontSize="12.5" FontWeight="SemiBold"
                       Foreground="{DynamicResource ChEmail}"/>
            <TextBlock Grid.Column="2" Text="{Binding EmailCount}" Classes="mono"
                       HorizontalAlignment="Right" Foreground="{DynamicResource Ink}"/>
          </Grid>
          <Grid ColumnDefinitions="60,*,30">
            <TextBlock Grid.Column="0" Text="Text" FontSize="12.5" FontWeight="SemiBold"
                       Foreground="{DynamicResource ChText}"/>
            <TextBlock Grid.Column="2" Text="{Binding TextCount}" Classes="mono"
                       HorizontalAlignment="Right" Foreground="{DynamicResource Ink}"/>
          </Grid>
          <Grid ColumnDefinitions="60,*,30">
            <TextBlock Grid.Column="0" Text="Voice" FontSize="12.5" FontWeight="SemiBold"
                       Foreground="{DynamicResource ChVoice}"/>
            <TextBlock Grid.Column="2" Text="{Binding VoiceCount}" Classes="mono"
                       HorizontalAlignment="Right" Foreground="{DynamicResource Ink}"/>
          </Grid>

          <Border BorderBrush="{DynamicResource Line}" BorderThickness="0,1,0,0" Padding="0,11,0,0">
            <Grid ColumnDefinitions="*,Auto">
              <TextBlock Grid.Column="0" Text="Estimated cost" FontSize="12"
                         Foreground="{DynamicResource Ink3}" VerticalAlignment="Center"/>
              <TextBlock Grid.Column="1" Text="{Binding CostTotal}" Classes="mono" FontSize="17"
                         Foreground="{DynamicResource Ink}"/>
            </Grid>
          </Border>
          <TextBlock Text="{Binding CostLine}" FontSize="11.5" TextWrapping="Wrap"
                     Foreground="{DynamicResource Ink3}"/>

          <Border Classes="notice privacy" IsVisible="{Binding AnyUnreachable}">
            <TextBlock Text="{Binding UnreachableLine}" FontSize="12" TextWrapping="Wrap"
                       Foreground="{DynamicResource Ink2}"/>
          </Border>
        </StackPanel>
      </Border>
    </Grid>
  </StackPanel>
</UserControl>
